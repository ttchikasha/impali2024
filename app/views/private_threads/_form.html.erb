<%= form_with(model: private_thread) do |form| %>
  <% if private_thread.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(private_thread.errors.count, "error") %> prohibited this private_thread from being saved:</h2>

      <ul>
        <% private_thread.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user1_id %>
    <%= form.text_field :user1_id %>
  </div>

  <div class="field">
    <%= form.label :user2_id %>
    <%= form.text_field :user2_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
