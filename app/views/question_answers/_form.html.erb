<%= form_with(model: question_answer) do |form| %>
  <% if question_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(question_answer.errors.count, "error") %> prohibited this question_answer from being saved:</h2>

      <ul>
        <% question_answer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :question_id %>
    <%= form.text_field :question_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :selected_answer %>
    <%= form.text_field :selected_answer %>
  </div>

  <div class="field">
    <%= form.label :answer %>
    <%= form.rich_text_area :answer %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
